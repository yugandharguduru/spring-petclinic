pipeline {
	agent any
	stages {
		stage('clone the git code') {
			steps { 
				git credentialsId: 'admin', url: 'https://github.com/yugandharguduru/spring-petclinic.git'
				ls -lrt
			    }
			}
			stage('im doing build'){
			steps {
			"mvn clean package"
			}
			}
		}
	}
	